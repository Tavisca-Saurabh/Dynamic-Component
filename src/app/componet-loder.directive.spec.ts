import { ComponetLoderDirective } from './componet-loder.directive';

describe('ComponetLoderDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponetLoderDirective();
    expect(directive).toBeTruthy();
  });
});
